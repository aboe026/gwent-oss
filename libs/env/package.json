{
  "name": "@gwent/env",
  "version": "0.1.0",
  "author": "Adam Boe",
  "description": "Parse process.env",
  "main": "build/src/env.js",
  "types": "build/src/env.d.ts",
  "scripts": {
    "build": "yarn g:tsc --build",
    "clean": "yarn g:rimraf build coverage test-results tsconfig.tsbuildinfo",
    "coverage-view": "yarn g:coverage-view",
    "lint": "yarn g:lint",
    "lint:fix": "yarn g:lint-fix",
    "test-unit": "yarn g:jest --test-type=unit",
    "watch": "yarn g:tsc --watch"
  },
  "dependencies": {
    "dotenv": "16.0.3",
    "envalid": "7.3.1"
  },
  "devDependencies": {
    "@types/jest": "29.5.0",
    "jest-junit": "15.0.0",
    "ts-jest": "29.0.5"
  }
}
