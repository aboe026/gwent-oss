{
  "name": "@gwent/constants",
  "version": "0.22.0",
  "author": "Adam Boe",
  "description": "Defined constants to share between microservices",
  "main": "build/src/constants.js",
  "types": "build/src/constants.d.ts",
  "scripts": {
    "build": "yarn g:tsc --build",
    "build-all": "yarn g:recurse @gwent/constants run build",
    "clean": "yarn g:rimraf build coverage results tsconfig.tsbuildinfo",
    "coverage-view": "yarn g:coverage-view",
    "lint": "yarn g:lint",
    "lint:fix": "yarn g:lint-fix",
    "test-unit": "yarn g:jest --test-type=unit",
    "watch": "yarn g:tsc --watch"
  },
  "devDependencies": {
    "@types/jest": "30.0.0",
    "@types/node": "24.12.0",
    "jest-junit": "16.0.0",
    "ts-jest": "29.4.6"
  }
}
