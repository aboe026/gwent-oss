{
  "name": "@gwent/msvs-ui",
  "version": "0.13.0",
  "author": "Adam Boe",
  "description": "React frontend to play Gwent.",
  "main": "src/index.ts",
  "scripts": {
    "build": "yarn g:npm-run-all --parallel build-tsc build-client",
    "build-tsc": "yarn g:tsc --build",
    "build-client": "yarn workspaces foreach --worktree --parallel --interlaced --topological-dev --exclude @gwent/msvs-ui run build",
    "build-image": "docker build --tag=gwent-ui:${IMAGE_TAG:-latest} --progress=plain --file=Dockerfile --build-arg VERSION=${IMAGE_VERSION:-dev} --build-arg BUILD=${IMAGE_BUILD:-0} ${BUILD_OPTIONS:-} ../../",
    "clean": "npm-run-all clean-children clean-root",
    "clean-root": "yarn g:rimraf build coverage results tsconfig.tsbuildinfo",
    "clean-children": "yarn workspaces foreach --worktree --parallel --interlaced --exclude @gwent/msvs-ui run clean",
    "coverage-view": "yarn g:coverage-view",
    "lint": "yarn g:lint",
    "lint:fix": "yarn g:lint-fix",
    "run-built": "cross-env CLIENT_DIR=../../libs/client/build IMAGES_DIR=../../images yarn node build/src/index.js",
    "start": "yarn g:ts-node src/index.ts",
    "test-unit": "yarn g:jest --test-type=unit",
    "watch": "yarn g:npm-run-all --parallel watch-tsc watch-client watch-built",
    "watch-built": "cross-env CLIENT_DIR=../../libs/client/build IMAGES_DIR=../../images yarn g:nodemon --watch build --watch libs/client/build/dynamic-env.js --exec 'yarn node build/src/index.js'",
    "watch-client": "yarn workspaces foreach --worktree --parallel --interlaced --exclude @gwent/msvs-ui run watch",
    "watch-tsc": "yarn g:tsc --build --watch"
  },
  "dependencies": {
    "@gwent/env": "workspace:*",
    "@gwent/utils": "workspace:*",
    "express": "5.1.0",
    "fs-extra": "11.3.0",
    "log4js": "6.9.1",
    "replace-in-file": "7.2.0"
  },
  "devDependencies": {
    "@gwent/client": "workspace:*",
    "@types/express": "5.0.0",
    "@types/fs-extra": "11.0.4",
    "@types/jest": "30.0.0",
    "cross-env": "7.0.3",
    "jest-junit": "16.0.0",
    "ts-jest": "29.4.0"
  },
  "workspaces": [
    "libs/*"
  ]
}
