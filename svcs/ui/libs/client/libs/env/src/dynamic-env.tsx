/**
 * Environment variables available to the broser. These are set by the UI server at runtime.
 */
window.env = {
  API_BASE_URL: 'TO_BE_REPLACED_BY_SERVER_AT_RUNTIME',
  WEB_SOCKET_PING_INTERVAL_SECONDS: 'TO_BE_REPLACED_BY_SERVER_AT_RUNTIME',
}
